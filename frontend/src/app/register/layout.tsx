export const metadata = {
  title: "Đăng Ký - Yu Ling Store",
  description: "Tạo tài khoản để bắt đầu sử dụng dịch vụ",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
